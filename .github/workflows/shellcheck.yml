name: shellcheck

on:
  pull_request:
    paths:
      - '**.sh'
      - '.github/workflows/**'
  push:
    branches:
      - master
      - staging
      - trying
  schedule:
    - cron: '00 01 * * *'

jobs:
  shfmt:
    name: shfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: shfmt
        shell: bash
        run: |
          . ci/scripts/install-shfmt.sh
          . ci/scripts/shfmt.sh check

  shellcheck:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: shellcheck
        shell: bash
        run: |
          sudo apt install shellcheck
          shellcheck ci/scripts/*.sh
