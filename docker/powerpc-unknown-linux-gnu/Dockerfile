# This file is @generated by xarch-codegen.
# It is not intended for manual editing.
#
# See tools/codegen/src/docker.rs for details.

FROM ubuntu:20.04

RUN apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
        binfmt-support \
        ca-certificates \
        curl \
        file \
        g++ \
        g++-powerpc-linux-gnu \
        libc6-dev \
        libc6-dev-powerpc-cross \
        make \
    && rm -rf /var/lib/apt/lists/*

COPY qemu.sh /
RUN /qemu.sh ppc

ENV CARGO_TARGET_POWERPC_UNKNOWN_LINUX_GNU_LINKER=powerpc-linux-gnu-gcc \
    CARGO_TARGET_POWERPC_UNKNOWN_LINUX_GNU_RUNNER="qemu-ppc -cpu Vger" \
    CC_powerpc_unknown_linux_gnu=powerpc-linux-gnu-gcc \
    CXX_powerpc_unknown_linux_gnu=powerpc-linux-gnu-g++ \
    OBJDUMP=powerpc-linux-gnu-objdump \
    QEMU_LD_PREFIX=/usr/powerpc-linux-gnu
